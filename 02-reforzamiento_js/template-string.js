const name = 'Deadpaul';
const real = 'Wade Winston';

const complete = name + ': ' +real;
const template = `${name}: ${real}`;

const html = `
<h1> Titulo </h1>
<p> Este es un parrafo </p>
`;

console.log(html)